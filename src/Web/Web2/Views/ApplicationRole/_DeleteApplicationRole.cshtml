@model string
@using Web.Models

<form asp-action="DeleteApplicationRole" role="form">
    @Html.PartialAsync("_ModalHeader", new ModalHeader { Heading = "Delete Application Role" })

    <div class="modal-body form-horizontal">
        Are you want to delete @Model?
    </div>
    @Html.PartialAsync("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
</form>

